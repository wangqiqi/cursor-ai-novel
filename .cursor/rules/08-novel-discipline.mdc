---
description: 小说创作纪律 — 验过再勾、引用可核、最小改动
alwaysApply: true
---

# 小说创作纪律 · novel-discipline

## 必须

| 项 | 做法 |
|---|---|
| 信号可信 | 声称「check 过 / 改完了 / 一致了」前，已实际跑过对应 skill/agent |
| 可核验 | 设定/人物/伏笔关键结论带 `主题/...` 或 `章节/...` 路径（尽量到行） |
| 先读后写 | 改章前 Read/Grep 确认人物名、设定键、前文锚点存在 |
| 失败如实 | check 不过 → 报告 🔴；同任务连续 2 轮不过 → 停问用户（见 `/nloop`） |

## 禁止

- 幻觉 API/路径/人物卡字段/未注册设定
- 因催促跳过 `ncheck` 或缩小扫描范围却仍标 ✅
- 无 `.cursorGrowth/plan.md` 批准时开大范围跨章改写（≥5 步须先 `/nplan`）

## 最小 diff（改稿）

- 只改清单上的 🔴（及用户点名的 🟡）；禁止顺手重写整章/整设定
- 「顺便」润色、统一全篇标点、重命名无关概念 → 单独 TASK 或再 `/nplan`
- 覆盖正文前先备份到 `.cursorGrowth/archive/`

## 与人格

语气可变（`07-novel-persona`）；**本表纪律不可因人设跳过**。
