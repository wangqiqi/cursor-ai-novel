---
description: 小说项目元规则——目录结构、命名规范、CHANGELOG 与 plan 工作板维护。
alwaysApply: true
---

# 小说项目 · 元规则 · novel-meta

## 〇、母版真源
- **母版仓库**：`cursor-ai-novel`（公开分发；文档只用相对路径/`git clone`，禁止写本机绝对路径）。
- **真源目录**：该仓库内的 `.cursor/`（见其 `.cursor/README.md`）。
- **新书建项**：先整目录复制母版 `.cursor/` 到新项目根，再执行 `/nnew`；禁止从某本具体小说拷贝特化内容冒充母版。
- **回灌**：通用技法经授权合入母版仓库；项目声口/偏好只用 `/nlearn` 写入该项目的 `.cursorGrowth/`。
- **脱敏**：`.cursor/` 内不得出现本机用户名、绝对路径、具体书名/角色高潮等非通用信息。

## 一、目录约定 (Flat Structure)
- `主题/`：**常驻**核心资产（世界观、主线、人物、母题、伏笔、节奏、约束、章前卡）。可含子目录 `人物/`、`章节卡/`；`章节复盘/` 仅常驻章末元数据/后记。
- `章节/`：小说正文（一章一目录，一节一文件）。
- `.cursorGrowth/check/`：当前 sprint 检测报告、批注、修改清单（git 忽略）。
- `.cursorGrowth/archive/`：闭合 sprint 归档（git 忽略）。
- `.cursor/`：通用框架（Rules/Skills/Agents/Commands）——消费仓从母版复制，保持干净。
- `主题/_meta/`：旧路径**回退**（无根文件时才读）；**禁止**再写入检测报告。

### 路径解析（优先已存在文件）

| 资产 | 优先 | 回退 |
|------|------|------|
| 章前卡 | `主题/章节卡/` | `主题/_meta/第NN章_章前卡.md` |
| 伏笔板 | `主题/伏笔板.md` | `主题/_meta/伏笔板.md` |
| 节奏窗 | `主题/节奏窗.md` | `主题/_meta/节奏窗.md` |
| 通用约束 | `主题/通用约束.md` | `主题/_meta/通用约束.md` |
| 检查报告 | `.cursorGrowth/check/` | 闭合后迁 `archive/` |

## 二、章节正文铁律
- **🔴 铁律**：正文文件永远只包含小说文稿。
- **检测报告 / 批注 / 修改清单** → `.cursorGrowth/check/`（sprint 闭合迁 `archive/`）。
- **禁止**把过程件写入 `主题/` 或 `章节/`。

## 三、工程规范
1. **CHANGELOG**：每次实质性修改必须逆序更新 `CHANGELOG.md`。
2. **plan 工作板**（真源 `.cursorGrowth/plan.md`，见 `config/workflow.json` → `plan_file`）：
    - 任务 ≥ 5 步必须先规划。
    - 工作板不是档案馆；任务完成后立即清理并归档。
    - 活跃内容控制在 150-300 行。
    - **禁止**在仓库根再维护第二份 `plan.md`。
3. **归档命名**：`YYYYMMDD_HHMMSS_功能_模块.md`。

## 四、AI 行为约束
- **中文交流**：默认使用中文。
- **工作区路径**：执行文件操作前确认当前工作区路径；**勿**把本机绝对路径写入母版文档或示例。
- **通用性**：`.cursor/` 内容严禁绑定具体项目，保持干净整洁、复制即用。

## 关联
- **价值观**: `00-novel-values.mdc`
- **归档**: `99-novel-archive.mdc`
- **人格 / 纪律**: `07-novel-persona.mdc` · `08-novel-discipline.mdc`
- **命令**: `/nplan` `/nrun` `/nloop` `/nhelp`（完整表见 `skills/novel-plan/reference/routes.md`）
- **配置**: `config/roles.json` · `config/workflow.json`
