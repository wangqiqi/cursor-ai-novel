---
description: 归档与版本管理规则——归档命名、CHANGELOG 模板、Tag 规范
globs: .cursorGrowth/archive/**/*.md, CHANGELOG.md
alwaysApply: false
---

# 归档与版本管理规则

按用户规则 #6、#7、#9 强制执行。

## 一、归档命名（强制）

所有归档文件放入 `.cursorGrowth/archive/`，命名格式：

```
YYYYMMDD_HHMMSS_功能_模块说明.md
```

| 部分 | 说明 | 示例 |
|------|------|------|
| `YYYYMMDD` | 日期（年月日） | 日期占位符 |
| `_HHMMSS` | 时间（时分秒） | 时间占位符 |
| `_功能` | 类型：基础设施/剧情/人物/设定/审校/合并 | `_基础设施` |
| `_模块` | 涉及模块：小说创作体系/第N章/<人物名>弧光 | `_小说创作体系` |
| `_说明.md` | 一句话具体说明 | `_建立.md` |

完整示例（占位）：

```
YYYYMMDD_HHMMSS_基础设施_小说创作体系_建立.md
YYYYMMDD_HHMMSS_剧情_<章节名>_完稿.md
```

## 二、归档内容

每次归档必须包含：

```markdown
# 归档 · <一句话说明>

## 元数据
- **归档时间**：YYYY-MM-DD HH:MM:SS
- **触发原因**：（剧情完稿 / 体系升级 / 重大修改 / 用户决策）
- **影响范围**：（文件清单）
- **关联版本**：（项目自定的版本号）

## 变更摘要
1. ...
2. ...

## 决策记录
- **决策 A**：选了 X 方案，原因是……
- **决策 B**：……

## 后续 TODO
- [ ] ...
```

## 三、CHANGELOG 模板（强制）

每次更新倒序写入 `CHANGELOG.md`：

```markdown
# CHANGELOG · <项目名>

> 倒序记录所有重要变更（最新在最上）。

---

## [vX.Y.Z] · YYYY-MM-DD HH:MM · <一句话标题>

### 新增
- <文件/功能> · 一句话说明

### 修改
- <文件> · 一句话说明

### 删除
- <文件> · 一句话说明

### 归档
- `.cursorGrowth/archive/YYYYMMDD_HHMMSS_..._..._...md` · 一句话说明

### 标签
`tag1` `tag2` `tag3`

---
```

## 四、Tag 规范（按用户规则 #7）

按功能分类：

| 类别 | Tag 前缀 | 示例 |
|------|---------|------|
| 新增功能 | `feat-` | `feat-character`、`feat-world` |
| 修复 | `fix-` | `fix-continuity`、`fix-style` |
| 文档 | `docs-` | `docs-meta`、`docs-rules` |
| 重构 | `refactor-` | `refactor-skill` |
| 剧情 | `plot-` | `plot-<章节标识>` |
| 人物 | `char-` | `char-<人物名>` |

## 五、版本号规范

> **建议**采用语义化版本（项目自定具体规则）：

- **主版本**（X.0.0）：全书完结
- **次版本**（0.X.0）：完成一个完整章节 / 一次重大体系升级
- **修订**（0.0.X）：小修改、bug fix、文案优化

## 六、git 提交流程（按用户规则 #8）

按用户规则 #8："不要随意进入到其他项目目录的仓库中进行 git 操作"

提交流程：

1. **改 CHANGELOG.md**（最上面）
2. **写归档文件**（如果是大变更）
3. **git add** 具体文件（不要 `git add .`）
4. **git commit** 信息格式：`<Tag>: <一句话说明>`
5. **git tag** `vX.Y.Z`（按本次变更的版本号）
6. **git status** 验证

## 七、检查清单

- [ ] 归档文件名符合规范
- [ ] 归档内容含元数据 + 摘要 + 决策 + TODO
- [ ] CHANGELOG 倒序更新
- [ ] Tag 与 CHANGELOG 中的标签一致
- [ ] git 提交前确认在正确仓库
