---
description: 归档与版本管理规则——归档命名、CHANGELOG 模板、Tag 规范
globs: .cursorGrowth/archive/**/*.md, CHANGELOG.md
alwaysApply: false
---

# 归档与版本管理规则

> 本规则是归档命名、CHANGELOG、Tag、提交纪律的**唯一真源**；母版内其他文件引用本规则，不另立口径。

## 一、归档命名（唯一文法）

所有归档文件放入 `.cursorGrowth/archive/`，命名文法：

```
YYYYMMDD_HHMMSS_<功能>_<模块>[_<细分>...].md
```

| 部分 | 说明 | 示例 |
|------|------|------|
| `YYYYMMDD` | 日期（年月日） | `20260818` |
| `_HHMMSS` | 时间（时分秒） | `_160300` |
| `_功能` | 类型：基础设施 / 剧情 / 人物 / 设定 / 审校 / 合并 / check / 去AI味 / 读者模拟器 / 同步 / 骨架重跑 | `_基础设施` |
| `_模块` | 涉及模块：小说创作体系 / 第N章 / <人物名>弧光；**可含下划线** | `_小说创作体系` |
| `[_细分...]` | 可选细分，允许继续追加 `_片段` | `_建立` |

**判定口径**：下划线分隔 **≥ 4 段**即合法。完整示例（占位）：

```
YYYYMMDD_HHMMSS_基础设施_小说创作体系_建立.md
YYYYMMDD_HHMMSS_剧情_<章节名>_完稿.md
YYYYMMDD_HHMMSS_check_第NN章_<维度>_<agent>.md
YYYYMMDD_HHMMSS_去AI味_第NN章_第SS节_原稿.md
```

> 长尾可自由追加分段，**不要**为了凑 4 段而省掉「功能」或「模块」。

## 二、归档内容

每次归档必须包含：

```markdown
# 归档 · <一句话说明>

## 元数据
- **归档时间**：YYYY-MM-DD HH:MM:SS
- **触发原因**：（剧情完稿 / 体系升级 / 重大修改 / 用户决策）
- **影响范围**：（文件清单）
- **关联版本**：（项目自定的版本号）

## 变更摘要
1. ...
2. ...

## 决策记录
- **决策 A**：选了 X 方案，原因是……
- **决策 B**：……

## 后续 TODO
- [ ] ...
```

## 三、CHANGELOG 模板（强制）

每次更新倒序写入 `CHANGELOG.md`：

```markdown
# CHANGELOG · <项目名>

> 倒序记录所有重要变更（最新在最上）。

---

## [vX.Y.Z] · YYYY-MM-DD HH:MM · <一句话标题>

### 新增
- <文件/功能> · 一句话说明

### 修改
- <文件> · 一句话说明

### 删除
- <文件> · 一句话说明

### 归档
- `.cursorGrowth/archive/YYYYMMDD_HHMMSS_..._..._...md` · 一句话说明

### 标签
`tag1` `tag2` `tag3`

---
```

## 四、Tag 规范

按功能分类：

| 类别 | Tag 前缀 | 示例 |
|------|---------|------|
| 新增功能 | `feat-` | `feat-character`、`feat-world` |
| 修复 | `fix-` | `fix-continuity`、`fix-style` |
| 文档 | `docs-` | `docs-meta`、`docs-rules` |
| 重构 | `refactor-` | `refactor-skill` |
| 剧情 | `plot-` | `plot-<章节标识>` |
| 人物 | `char-` | `char-<人物名>` |

## 五、版本号规范

> **建议**采用语义化版本（项目自定具体规则）：

- **主版本**（X.0.0）：全书完结（须情节尺已绿；出版尺未绿不得升主版本，见 `skills/novel-plan/reference/universal-gates.md` §7）
- **次版本**（0.X.0）：完成一个完整章节 / 一次重大体系升级
- **修订**（0.0.X）：小修改、bug fix、文案优化

### CHANGELOG 纪律

- 一版号一条；禁止同一 `[vX.Y.Z]` 并行两条
- `[unreleased]` 必须在下一正式版并入后删除
- 倒序；新条目时间戳不得早于下一条
- 标签与 git tag 一致

## 六、git 提交流程

> **铁律**：不要进入**其他项目**的仓库执行 git 操作；只在当前项目根操作。
> **推送是可选动作**：`commit` 默认可做；`tag` / `push` 仅当 plan 的完成定义要求、且用户未禁止时执行（与 `/nloop` 一致）。无 remote 时只 commit，不 push。

提交流程：

1. **改 CHANGELOG.md**（最上面）
2. **写归档文件**（如果是大变更）
3. **git add** 具体文件（不要 `git add .`）
4. **git commit** 信息格式：`<Tag>: <一句话说明>`
5. **git tag** `vX.Y.Z`（按本次变更的版本号）
6. **git status** 验证

## 六·五、快照与回滚（改稿安全网 · 强制）

> **规则**：任何**覆盖正文**的动作（`novel-rewrite` 各轮、`novel-line-rewriter` 改稿）之前，必须先留快照。
> 只备份不留恢复路径，等于没有安全网——回滚走下面的命令，不要手工拼文件名。

```bash
python3 .cursor/tools/snapshot.py snapshot --note 去AI味第03章   # 覆盖前
python3 .cursor/tools/snapshot.py diff latest                   # 改了多少
python3 .cursor/tools/snapshot.py verify latest                 # 快照自身是否完好
python3 .cursor/tools/snapshot.py restore latest --yes           # 改坏了：回滚
```

- 快照落在 `.cursorGrowth/archive/YYYYMMDD_HHMMSS_快照_<note>/`（含 `manifest.json`：逐文件 sha256 + 字数）。
- `restore` 会**先给当前状态补一份安全快照**，再回滚，因此回滚本身可再回滚。
- 默认只操作 `章节/`；需要带上设定时用 `--paths 章节 主题/世界观.md`。
- 推荐节奏：**一个任务一个快照**（改稿前），不要每节都拍；快照过多时清理旧目录即可。

## 七、检查清单

- [ ] 归档文件名符合规范（`YYYYMMDD_HHMMSS_<功能>_<模块>[_<细分>...].md`，≥4 段）
- [ ] 归档内容含元数据 + 摘要 + 决策 + TODO
- [ ] 覆盖正文前已留快照（`snapshot.py snapshot`），且能 `diff` 出来
- [ ] CHANGELOG 倒序更新
- [ ] Tag 与 CHANGELOG 中的标签一致
- [ ] git 提交前确认在正确仓库
