# 通用约束 · <项目名>

> **模板位置**：`.cursor/templates/constraints.md`
> **项目副本**：复制为 `主题/通用约束.md`（兼容旧路径 `主题/_meta/通用约束.md`）
> **定位**：本项目**唯一的阈值真源**。`.cursor/` 只负责检测逻辑，不写死数值。
> **填写时机**：`/nnew` 骨架阶段由 scaffold 生成默认值；写第一章前按需覆盖。

> **缺省原则**：表中标「默认」的行若留空，工具链按默认值执行；标「必填」的行必须填写。

---

## 一、篇幅与结构

### 1.1 颗粒度档（先定「章」是什么，再谈其他）

> **这是全项目最重要的一个约定**：`章` 的含义由本档决定，`节拍表 / 节奏窗 / 章前卡 / chapter-brief` 全部按它解释。
> 选错档的典型症状：网文按母版默认（大章）写 → 每章 3 万字、读者一天看不完；出版按网文档写 → 章节碎成上千条。

| 档 | 适用 | 单章字数 | 单章节数 | 「一章」实际是 | 节奏窗 N |
|----|------|---------|---------|--------------|---------|
| **A · 网文连载** | 平台日更 / 追读制 | 2000–4000 汉字 | 1–2 节 | 读者的「一话」 | 总章数（可数百–上千） |
| **B · 出版长篇** | 实体出版 / 单本长篇 | 6000–12000 汉字 | 3–6 节 | 书里的「一章」 | 实际章数 |
| **C · 大章 / 卷**（母版默认） | 大章制 / 单章多场景 | 30000–60000 汉字 | 5–15 节 | 近似「一卷」 | 大章数（节奏窗按大章） |

| 项 | 值 | 说明 |
|----|-----|------|
| **颗粒度档** | A / B / C | 选一档；混合制请写明例外 |
| **篇幅档** | 短篇 / 中篇 / 长篇 / 史诗 | 来自 Q2 |
| **计划总章数 N** | 见 `主题/总览.md`（**唯一登记处**，本表不复制数值） | 按所选档的「章」计数；换算见 `skills/novel-plot/reference/opening-protocol.md` |
| **单章节数窗** | 按所选档（见 1.1） | 超出需在章前卡说明 |
| **单章字数窗** | 按所选档（见 1.1） | — |
| **单节目标字数** | 默认 2000–5000 汉字 | 见下节硬上限 |
| **单节硬上限** | 默认 5000 汉字 | 超过须用户特批并记录理由 |
| **叙事结构** | 三幕 / 起承转合 / 英雄之旅 / 自由 / 多线 | 来自 Q5 |

> **N 只登记一次**：`主题/总览.md` 的「计划总章数 N」是唯一数值来源；`主题/节奏窗.md` 与本表只**引用**，不另填数。
> 改档等于改全书标尺 —— 必须重算节奏窗，并走 `/nplan`（属"核心设定变更"）。

## 二、密度硬约束（`01-novel-language.mdc` 执行）

| 维度 | 阈值（默认） | 本项目 | 处置 |
|------|-------------|--------|------|
| **加粗密度** | > 130 / 100 字 → 🔴 |  | 仅保留白名单加粗 |
| **链式句密度** | 见本项目 |  | 拆短句、加留白 |
| **母题数字复读** | 见本项目 |  | 改动作折射 |

## 三、白名单 / 黑名单（项目特化）

### 3.1 加粗白名单（任何改稿模式都不得删除）

> **本表是加粗白名单的唯一真源。** `主题/世界观.md` 的母题表若另有登记，须与本表一致；**冲突以本表为准**。
> 工具链不硬编码项目白名单；`novel-line-scanner` / `novel-line-rewriter` 改稿前必读本表。

- `` `**<母题数字>**` ``：
- `` `**<关键概念>**` ``：
- `` `**<重锤词>**` ``：

### 3.2 套词黑名单

> `01-novel-language.mdc` 已给通用黑名单；此处只补**本项目特有**的禁忌表达。
> **本表被 `tools/check_manuscript.py` 直接读取**（机械校验时会报出命中）；一条一行，不要写说明文字。

- 
- 

## 四、伏笔回收距离（`novel-continuity` / `novel-check` 执行）

| 项 | 值 | 说明 |
|----|-----|------|
| **默认回收距离** | 短篇 20 章 / 长篇 50 章 | 母版默认，可被下行覆盖 |
| **本项目回收距离** |  | 留空则按篇幅档取默认 |
| **单条伏笔覆盖** | 见 `主题/伏笔板.md`「预期回收距离」列 | 单条优先于全局 |

> **超期判定**：`当前章号 − 埋设章号 > 该伏笔的预期回收距离` 即告警。不要用固定「10 章」这类硬编码。

## 五、读者奖励密度（`02-novel-plot-design` 执行）

| 项 | 值 | 说明 |
|----|-----|------|
| 每章小爽点 | ≥ 1（类型轮换） | 强制（可被 `主题/风格档.md` §三 声明降级） |
| 中/大爽点间隔 | 每 3–5 章 1 个 | 默认 |
| 开篇窗内密度 | 高于中段 | 窗的起止见 `主题/节奏窗.md` |
| **单章实质推进项上限** | **1** | 硬上限。配额制防"剧情加速"——把三章的量压进一章是最常见的崩坏方式 |
| **连续快档上限** | **2 章** | 连续 3 章"快"→ 🟡（读者会疲） |
| **事件冷却** | 冲突刺激 2 · 关系深化 1 · 势力经营 2 · 世界观铺陈 3 · 张力升级 2（章） | 同类型事件两次触发的最小间隔，防套路化 |

> 以上四项由 `python3 .cursor/tools/check_continuity.py` 按 `主题/连续性台账.md` §五 机械核对；
> 台账同时承载角色状态 / 境界层级 / 资源归属 / 子线进度（§一–§四）与豁免账本（§六）。
> **机械核对跑在 LLM 评审之前**，结论并入 8 维评审输入，不重复花 token。

## 六、视角与文体约定

| 项 | 值 |
|----|-----|
| **叙事视角** | 来自 Q4 |
| **视角切换标记** | 合法：行内 `> 【视角：<人物> · <地点> · <时间>】`；严禁 `##` 切块（`05-novel-style.mdc`） |
| **合法 `##` 标题** | 仅 `## 本节完` / `## 本章完` |
| **标点 / 数字** | 全角标点；数字 ≤ 10 用中文、> 10 用阿拉伯 |
| **可接受方言层** | 允许出现的方言/口语词范围（市井口语类原型需在此登记，否则读者看不懂即算 🔴） |
| **本项目特有文体禁忌** |  |

## 七、特化禁忌（本项目）

> 只写**本书**特有的禁忌；通用红线在 `00-novel-values.mdc`，不要在此重复。

- 
- 

## 八、类型工艺（`novel-genre` 执行 · 按 Q1 派发）

> 类型约束在**章前卡字段 + 本节阈值 + `novel-check` 的既有维度**三处落地；不新增检查维度。
> 加载了哪份参考、抽出了什么阈值，登记在这里——否则类型工艺只是"读了不用"。

| 项 | 值 |
|----|-----|
| **已加载参考** | 推理 `mystery-fairplay` / 言情 `romance-arc` / 体系 `power-system` / 历史 `historical-accuracy` / 喜剧 `comedy-craft`（最多 3 份） |
| **主线类型**（冲突时优先） |  |
| **类型阈值 1** | 例：推理「核心线索数 / 红鲱鱼数」=  |
| **类型阈值 2** | 例：言情「甜虐比」=  |
| **类型阈值 3** | 例：体系「层级数 / 越级代价」=  |
| **类型自检项**（从参考抽出的可勾选项） |  |

## 九、风格（`novel-style` 执行 · 项目风格契约）

> **本节的数值与豁免细节在 `主题/风格档.md`，此处只登记入口**，避免同一事实存两份。
> 风格层可**声明式覆盖** `01-novel-language.mdc` §二.3 与 §一 的部分阈值——但**必须先在此登记摘要、且在风格档 §三 写明理由**，否则偏离一律按 🔴 处理。

| 项 | 值 |
|----|-----|
| **主原型** | 代号（代表作家）：见 `主题/风格档.md` §一 |
| **副原型**（最多 1 个） |  |
| **一句话风格定位** |  |
| **目标句长区间** |  |
| **已声明的豁免**（条数 + 涉及指标） | 见 `主题/风格档.md` §三 |
| **不得豁免** | `00-novel-values.mdc` 全部 · 正文不含元数据 · 不美化暴力 · 不照搬任何作家文本 |

> 复核命令：`python3 .cursor/tools/check_manuscript.py --style`

## 变更记录

| 日期 | 原因 | 变更 |
|------|------|------|
|  | 初建（scaffold） | 由 `.cursor/templates/constraints.md` 生成 |
